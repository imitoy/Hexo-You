<style>
  @import url('/css/header-card.css');
</style>

<mdui-card variant="filled" class="header-card">
  <div class="header-title">Archives</div>
  <div class="header-content">All archived posts</div>
</mdui-card>

<% const archivePosts = getArchiveYears(); %>

<% Object.keys(archivePosts).reverse().forEach(year => { %>
  <h1 class="posts-year"><%= year %></h1>
  <mdui-list class="posts-list">
    <% archivePosts[year].forEach(post => { %>
      <mdui-list-item href="<%- url_for(post.path) %>">
        <div class="mdui-list-item-content">
          <div class="mdui-list-item-title"><%= post.title %></div>
          <div class="mdui-list-item-text"><%= post.date.format('YYYY-MM-DD') %></div>
        </div>
      </mdui-list-item>
    <% }) %>
  </mdui-list>
<% }) %>

<script>
  const archives = document.querySelector('.navigation-drawer-list-archives');
  if (archives) archives.active = true;
</script>
